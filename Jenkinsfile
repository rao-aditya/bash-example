pipeline {
    agent any
    stages {
        stage('Run Script') {
            steps {
                     bash 'chmod +x script.sh'
                     bash './script.sh'
                   }
                }
            }
         }
